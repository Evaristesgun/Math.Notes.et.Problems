name: Deploy GitHub Pages

on:
  workflow_run:
    workflows:
      - Build LinearAlgebra
      - Build ExPair PDF
    types:
      - completed

jobs:
  deploy:
    # only deploy if build succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4
      with:
        ref: main

    # ===== Wait for latest commit =====
    - name: Pull latest changes
      run: |
        git pull origin main

    # ===== Build website structure =====
    - name: Prepare Pages directory
      run: |
        rm -rf public
        mkdir -p public/LinearAlgebra
        mkdir -p public/ExPair

        # copy PDFs if they exist
        if [ -f LinearAlgebra/main.pdf ]; then
          cp LinearAlgebra/main.pdf public/LinearAlgebra/
        fi

        if [ -f ExPair/main.pdf ]; then
          cp ExPair/main.pdf public/ExPair/
        fi

        # optional homepage
        if [ -f index.html ]; then
          cp index.html public/
        fi

    # ===== Deploy =====
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: public
        publish_branch: gh-pages
